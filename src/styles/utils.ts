export const glowCol = "bg-gradient-to-r from-blue-500 to-purple-500";

// Updated shadow with colors that work better with the new backgrounds
export const shadow = "0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)";

// Alternative shadow for dark mode compatibility
export const shadowDark = "0 4px 6px -1px rgba(255, 255, 255, 0.1), 0 2px 4px -1px rgba(255, 255, 255, 0.05)";